listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/

# Development-only local broker. Configure authentication and TLS before
# exposing this broker outside a trusted development network.
